{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/favicon.ico", "destination": "/favicon.svg" }],
  "functions": {
    "api/dashboard-bundle.ts": {
      "maxDuration": 60
    }
  }
}
