# Copie para env.google (não commite credenciais) e carregue antes do export:
#   export $(grep -v '^#' scripts/env.google | xargs)
#   python3 scripts/export_bundle_from_sheet.py

GOOGLE_SHEETS_SPREADSHEET_ID=1b__ZMvvF4JM6v2vSPF-2iIfItZTOgrlASixP5yafKW4
GOOGLE_APPLICATION_CREDENTIALS=/caminho/absoluto/conta-servico.json

# Só se precisar forçar o nome da aba (senão o script tenta «dados brutos dashboard» → «Página1» → 1.ª aba):
# GOOGLE_SHEETS_WORKSHEET_NAME=Página1
